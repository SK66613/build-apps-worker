{
  "name": "build-apps-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "wrangler": "^4.64.0",
    "typescript": "^5.6.3",
    "@cloudflare/workers-types": "*"
  }
}
